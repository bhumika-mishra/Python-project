def missing(arr, index=0):
    m_number = index + 1
    if arr[index] != m_number:
        return m_number
    return missing(arr, index + 1)
n = [1,2,4,5,6] 

missing_val = missing(n)
print(f"The missing number is: {missing_val}")